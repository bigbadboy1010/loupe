# Deprecated static file kept only as documentation.
# docker-compose now builds ./coturn and starts coturn through docker-entrypoint.sh,
# injecting TURN_SECRET/TURN_REALM from the same .env values used by loupe-signaling.
# Do not mount this file for production, otherwise secrets will drift.
